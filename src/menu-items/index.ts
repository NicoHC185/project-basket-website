import { IMenuList } from "interfaces";
import NBA from "./nba";

// ==============================|| MENU ITEMS ||============================== //

const menuItems: { items: Array<IMenuList> } = {
  items: [NBA],
};

export default menuItems;
