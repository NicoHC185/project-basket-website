import { IMenu } from "interfaces";
import NBA from "./nba";

// ==============================|| MENU ITEMS ||============================== //

const menuItems: { items: Array<IMenu> } = {
  items: [NBA],
};

export default menuItems;
